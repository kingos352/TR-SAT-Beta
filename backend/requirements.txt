fastapi>=0.110.0
scipy>=1.12.0
uvicorn>=0.28.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
sqlalchemy>=2.0.0
httpx>=0.27.0
sgp4>=2.23
skyfield>=1.48
python-dotenv>=1.0.1
pytest>=8.0.0
websockets>=12.0

# Optional — GPU acceleration for conjunction screening (NVIDIA CUDA)
# RTX 40xx / 30xx (CUDA 12.x):  pip install cupy-cuda12x
# Older cards (CUDA 11.x):       pip install cupy-cuda11x
# Without this package the code falls back to NumPy automatically.

